# code-generation — 観察日誌（Construction 3.5、unit-major）

> ステージ実行中の解釈・逸脱・トレードオフ・未確定事項を ISO 8601 タイムスタンプ付きで追記する。手編集はしない。

## Interpretations

- 2026-08-22T12:15:00Z — Plan Approval に先立ち、計画の形を左右する 2 点（Bolt ブランチと aidlc 記録のコミット方法 / ゴールデン配置）を質問票の Q1・Q2 として先に人間へ問う; project.md の是正学習「上流成果物間の矛盾は人間へ裁定」（C7 の `tests/goldens/` と既存 `tests/golden/upstream-3c3146cf/` の並立）に従う
- 2026-08-22T12:15:00Z — 本ハーネス（2.6.54）の CLI には Plan Approval 指紋の機械ガードが無い（`grep fingerprint .claude/tools` は review 指紋のみ）; ステージ定義どおり `[Approval Fingerprint]` と `Approve Plan` の儀式は守り、非ゲート質問として `aidlc-log.ts decision/answer` で記録する

## Deviations

- 2026-08-22T12:40:00Z — 委任ブリーフはルール束・計画・テスト手順を逐語で連結した記録内ファイル `developer-brief-1.md`（86KB）にまとめ、委任プロンプトでは先頭 2 行のマーカーと「全文 Read」指示 + 要点再掲にとどめた; ステージ定義の「ルール束を逐語で貼る」をファイル経由で満たす（トークン節約、Fable 5 委任方針）
- 2026-08-22T12:40:00Z — Bolt B1 は worktree を使わず `main-sync` から `bolt/b1-u1-canon-json-goldens` を切った（Q1 = A、PR 直列・aidlc 記録を先頭コミットに同乗）; `aidlc-bolt.ts start --name B1 --batch 1`（worktree なし）で BOLT_STARTED を記録

## Tradeoffs

- 2026-08-22T12:40:00Z — 委任を 2 回（Step 1〜16 / Step 17〜19）に分け直列化; 1 回にまとめると文脈が長くゴールデン採取の品質が落ちるため。同一承認・同一指紋の下で行う

## Open questions

- 2026-08-22T12:15:00Z — upstream ピン `3c3146cf` の dist ツールは raw.githubusercontent.com から取得可能（HTTP 200 実測）; 採取スクリプトはネットワーク前提になる（A3 はオーナー環境での実行を許容）
- 2026-08-22T13:15:00Z — [Interpretations] Plan Approval の指紋は `code-generation-plan.md` のバイト列そのものに掛かる（`aidlc-plan-approval-guard.ts` PreToolUse hook + `approvalFingerprint(plan, instructions, contract)`）ため、開発エージェントが計画のチェックボックスを `[x]` に更新すると委任 2 の dispatch が拒否された; 計画本文を承認時のバイト列（50e4ed7）へ戻して `verify` を ok にし、Step の完了状況は `code-summary.md` に記録する方針へ変更（ステージ定義「チェックボックスを完了にする」との相互作用 — 学習候補）
- 2026-08-22T13:25:00Z — [Deviations] code-summary.md と traceability.json を委任 2 の完了前に下書きとして produces パスへ置いたところ、engine が U1 code-generation を「成果物で settled」と判定して次ユニット（u10）へ進み error を返した; 下書きを scratchpad へ退避して run-stage に戻し、`unit start --stage code-generation --unit u1` を遅れて記録。教訓: produces に数えられるファイルは完了まで置かない（下書きは別名・別置き）
- 2026-08-22T13:35:00Z — [Open questions] 委任 1 の要確認 4 件をゲートで提示する: (1) 公開面に `Indent` / `KeyOrder` を追加（entities.md の閉じた列挙に忠実、計画 §2 より 2 項目多い）(2) `ParseError` / `ToValueError` への `std::error::Error` 手実装（前例なし — house style 裁定、coding-rules のエラー規則ドラフトへ一文追加の価値）(3) 深さ上限の実効値 127 段受理（security-design §2 / NFR4.3 の「128 超」文面との差）(4) `serde_json` `float_roundtrip` フィーチャ追加（tech-stack-decisions §2 依存差分表の更新）
- 2026-08-22T13:35:00Z — [Interpretations] Rust での TDD Red は「コンパイルは通るが値が誤るスタブ + 失敗テスト」で `test result: FAILED` 行を記録する方式を採用（新規型はコンパイルエラーで要約行が出ないため）。退行防止のガードテスト（ファサード公開面 2 本）は Red を経ていないことを報告に明記 — 学習候補（TDD 証跡の取り方）
- 2026-08-22T14:00:00Z — [Deviations] 委任 2 の最終報告（§8）はコンダクタに届かなかった（idle 通知のみ、再送依頼 2 回後も未着・エージェント終了）; 成果はコミット 2 件・コーパス・比較器テスト・コミット本文の TDD 記録から直接レビューして code-summary に記載した。教訓: 委任の戻り値は成果物ファイル（例: `<record>/.../developer-report-N.md`）へも書かせると通知経路に依存しない
- 2026-08-22T14:05:00Z — [Interpretations] 委任 2 の最終報告が遅れて到着（成果物確定・レビュー依頼後）。code-summary は REVIEW_REQUESTED 後のため編集せず、追加情報は本日誌とゲート提示に載せる: (a) TDD 証跡 `cargo test -p canon-json --test golden_corpus_read` Red `FAILED. 1 passed; 8 failed` → Green `ok. 9 passed`（コミット本文と一致）(b) README の part-1 追記表 2 セル（「（後続 Bolt）」）を訂正 — 追記のみ方針からの軽微な逸脱、upstream 由来の既存部分は不変 (c) C7 レイアウトに exit / stderr / case.json を追加（contract-summary へ反映済み）(d) normalization.json の規則順序を定義（`rule_application` 新設、既存 4 規則の本文は不変）(e) `regex` は dev-dependency（ランタイム依存 3 つは不変）(f) ワークスペース合計 338 テスト緑
- 2026-08-22T14:05:00Z — [Open questions] upstream ピン 3c3146cf の `set-autonomy` は状態ファイルに `Construction Autonomy Mode` 行が無く構造的に失敗する（既知バグ M12、逸脱台帳 #2 と同根）— amadeus-ng が再現するか修正するかは U7 でオーナー裁定（逸脱台帳 #2 は「修正」と記録済み; ゴールデンの期待値分岐はこの 1 点のみ）
- 2026-08-22T14:10:00Z — [Open questions] U1 code-generation レビュー READY（Major 1 / Minor 5）。トリアージ: #1 Major NFR2.1「失敗を PR 履歴で確認できる」vs 実態（Red は code-summary §2c の散文証跡、Red/Green 同一コミット）→ ゲートで (a) 証跡方式を散文記録に改める or (b) 後続 Bolt で Red を独立コミットにする運用を裁定; #2 Minor 公開面 17 項目のうち `MAX_DEPTH` と `to_value` の `?Sized` が逸脱台帳に未記載 → ゲートで 17 項目として承認、台帳追記は凍結解除後; #3 Minor 比較器の「欠落一覧が空でない」assert は後続で欠落が解消すると落ちる罠 → U6/U7 着手前に修正（件数を provenance と突合に置換）; #4 Minor `<CLONE>` regex が広い → U6/U7 採取前に錨を打つ; #5 Minor フック区分（許可/拒否/無視）の該当・N/A を README 表に明示 → U7; #6 Minor「serde_json は 128 段を受理」は**事実誤認**（コンダクタ実測: depth 127 ok / 128 err "recursion limit exceeded"）— 実装・説明とも正しい、却下
- 2026-08-22T17:08:00Z — [Interpretations] Bolt B1 ゲート Approve（オーナー）。裁定: NFR2.1 の TDD 証跡は code-summary への散文記録を正式とする（「PR 履歴で確認」文面は security-requirements.md NFR2.1 で U9 / ステージゲート時に改訂、traceability の NFR2.1 は現行 target のまま注記）; `std::error::Error` 手実装を公開エラー型の house style として認める（coding-rules エラー規則ドラフト（evidence.md）へ一文追加 — U10 で正本化）; 公開面 17 項目（Indent / KeyOrder / MAX_DEPTH / to_value ?Sized 含む）、float_roundtrip、regex dev-dep、README 2 セル訂正を承認扱い
- 2026-08-22T17:08:00Z — [Open questions] 次 Unit 以降への引き継ぎ: 比較器の空でない assert（#3）・`<CLONE>` regex の錨（#4）・フック区分 N/A 表（#5）は U6 / U7 着手前に対処; set-autonomy の upstream 欠落（M12）の再現/修正は U7 で裁定
- 2026-08-22T17:35:00Z — [Interpretations] U10（packaging）の Testing Contract（tdd / standard）は「設定の事実を機械検査する bash スクリプト（verify-ci-governance.sh）を先に書き現状で Red → 設定変更で Green」と写した（プロダクト層が無いため）。Plan Approval 済み（指紋 7f0e1353…）。委任は PR #24 マージ後に main から B2 ブランチを切ってから（Q1 = A）— それまで U10 の記録変更はローカル未コミットのまま持ち越し、B2 の先頭コミットにする
- 2026-08-22T23:02:00Z — [Interpretations] PR #24 を merge queue に投入（`gh pr merge` は auto-merge 無効で拒否されたため GraphQL `enqueuePullRequest` を直接実行）→ 23:00Z に squash-merge（9c4ee51）。Step 0: `origin/main` から `bolt/b2-u10-ci-governance` を作成（ローカルの `main` は別 worktree にチェックアウト済みのため `git switch -c … origin/main`）、U10 設計の記録を先頭コミットに
- 2026-08-22T23:30:00Z — [Open questions] U10 委任 3 完了（9 コミット、ゲート全緑、verify 15/15）。発見: (1) llvm-cov は絶対パスを記録するため承認済み除外 regex `^modules/...` は不活性 — `(^|/)` に訂正（正本更新はゲートで）(2) NFR2.4「差 0.00pp」未達: PBT シード固定は効いたが `fs_workspace_lock.rs:237` を並行テストが踏むかのジッタ 1 行（0.0175pp）> TOLERANCE 0.01 → 裁定（A 0.02 / B テスト追加で 0.01 維持 / C 0.01 のまま）をゲートで (3) ruleset 適用の順序は「PR 作成 → PR の CI 緑 → ruleset 適用 → queue 投入」が安全
- 2026-08-22T23:34:00Z — [Open questions] U10 code-generation レビュー READY（Major 1 / Minor 3）: #1 Major NFR2.4 未達（TOLERANCE 裁定）/ #2 Minor 0.02 は余裕が薄い（実測 8 回は少ない、上限は未保証）→ 暫定 0.05 を推奨案に / #3 Minor `@master` toolchain の CI 実挙動は PR 初回 CI で確認 / #4 Minor 除外 regex の正本（tech-stack-decisions §1 / security-design §4）が旧リテラルのまま → 凍結成果物のため U9 の文面更新に引き継ぎ、code-summary §5/§6 に記録済み
- 2026-08-22T23:35:00Z — [Interpretations] Bolt B2 ゲート Approve（オーナー）。裁定: TOLERANCE は暫定 0.05（既知の 1 行ジッタ 0.0175pp に余裕、従来 0.5 の 10 倍厳格; U3 のロック退役（ADR-007）後に 0.01 へ引き締める — U3 への引き継ぎ）; ruleset 適用は PR の CI 緑後にコンダクタが実行（gh はオーナーアカウント）。code-summary は終末レビュー受領後のため編集せず、裁定は本日誌・コミット本文・U3/U9 の引き継ぎに記録
- 2026-08-22T23:41:00Z — [Deviations] PR #25 初回 CI で check / coverage / audit が赤: `dtolnay/rust-toolchain@master` は `toolchain:` 入力必須で `rust-toolchain.toml` を自動では読まない（設計 security-design §2 の前提が誤り — code-summary §8 (c) で既知ギャップとしていた）。修正: `scripts/governance/toolchain-inputs.sh` がファイルから channel / components を導出し step 出力でアクションへ渡す（正本は 1 つのまま）。検査スクリプトの `ci-toolchain-file-driven` も「導出経由・リテラル禁止」に更新。コンダクタが直接修正（小さく局所的、Build and Test のループバック相当）
- 2026-08-22T23:43:00Z — [Interpretations] PR #25 の CI（toolchain 修正後）4 ジョブ緑。Step 10: ruleset「main」に required_status_checks（check / quint / coverage、strict）を適用（オーナー承認 A、`gh` はオーナーアカウント）。前後 JSON を `code-generation/ruleset/{before,after}.json` に保存、verify --with-ruleset 16/16 PASS。以後は merge queue 投入で正常系受入（NFR2.1）を確認する
- 2026-08-22T23:45:00Z — [Interpretations] PR #25 を merge queue に投入 → required checks（check / quint / coverage）が merge_group イベントで実行され緑 → squash-merge 完走（23:44:17Z）。NFR2.1 の正常系受入（緑 PR が queue を通って完走）と否定系は構造上成立（required checks 無しでは queue が merge できない）— Step 11 完了、Bolt B2 終了。`strict` × ALLGREEN の相互作用（reviewer 所見 e）は本実地で問題なし
- 2026-08-23T00:05:00Z — [Interpretations] オーナー指示で PR #24 / #25 のコメントをサルベージ: #24 は bot 2 件（Bugbot 使用量上限、CodeRabbit ファイル数超過でスキップ）のみ。#25 は CodeRabbit 15 件（Major 8 / Minor 6 / Trivial 1）。即時対応: 日誌タイムスタンプの UTC 修正（JST 表記の混入）、bolt-plan 詳細節の B 番号振り直し、verify に workspace member の lint 継承検査を追加、ruleset スクリプトの --dry-run 出力契約、brief の Step 0 履歴メモ、凍結文書の上書き記録 `superseding-decisions.md`（TOLERANCE 0.05 / 除外 regex / ruleset 適用済み / 冪等判定 / PBT シード / FR9.6 誤記 / bash -n）。凍結文書本文の修正はステージゲート前の回復レビュー 1 回にまとめる。以後、マージ済み PR のコメントは Bolt 完了時に必ず回収する（学習候補）
- 2026-08-23T00:40:00Z — [Interpretations] オーナー指示 2 件: (1) PR コメントを無視しない → PR #25 の CodeRabbit 15 スレッドすべてに対応内容を返信し resolve、凍結文書の本文も修正（回復レビューをステージゲート前に実施）(2) amadeus-dlc/amadeus の ci.yml に倣いレビュースレッドのゲートを導入 → review-thread-resolution ジョブ + CI Success 集約 + 再評価ワークフロー、required checks に CI Success を追加（superseding #9）
- 2026-08-23T00:25:00Z — [Interpretations] PR #26（レビュー引き取り + レビュースレッドゲート + required checks 4 つ）が merge queue を完走（2026-08-23T00:19Z）。以後の PR は未解決レビューコメントがあると CI Success が赤になりマージできない
- 2026-08-23T02:45:00Z — [Interpretations] [u2-domain-es-core] B3 計画: 委任 2 回直列（workflow_definition / Repository 側 → orchestration 側）、旧 API は削除（後方互換なし — オーナー裁定）、WorkflowDefinitionId は harness.json の name（前提 P1、framework 接頭辞なし）、DefinitionRevision は 3 入力の正準 JSON sha256。ITF 準拠テストは合成 StageEntry 列で集約を作る `start_with_entries` を公開 API に追加（設計からの差分として計画 §2 に記録）
- 2026-08-23T02:50:00Z — [Open questions] [u10-ci-governance] 回復レビュー（iteration 2）NOT-READY: Critical 1（code-summary に review-thread gate が未記載）/ Major 2（traceability の target に全角注記が連結され実在ファイルとして解決できない — センサー FAIL / 4 コンテキスト化未反映）。実装は verify-ci-governance 20/20 PASS で健全。本文は凍結中のためゲートの Request Changes 経路で適用（pending-revision.md）。学習候補: traceability の target は必ずファイルパス単体にする（注記は別欄）
