# 完了時の検証根拠の保存・投影

## 実装した範囲

承認済みU2計画Step 4・8のValidation Basisを、報告時の入力観測から単一のReportedイベントへ保存し、通常RMUからSTAGE_COMPLETED監査へ描く経路へ接続した。報告更新の成功戻り値はunitを維持し、報告後の表示は既存のID指定Queryを使う。監査を再投影するときに現在のファイルから指紋を取り直さない。

入力境界は本家a277af21のグラフ契約・入力/出力・条件・任意成果物を扱う。正準JSONは既存canon_jsonを使う。配布から既に採取済みのvalidation-basis.jsonの22観測（合成16境界とbugfixの実配布6工程）に対して全文一致した。構造と内容の指紋、所在、件数、任意ファイルの欠落、ディレクトリ、CodeKB、生成元の欠落・重複を含む。元の採取データは変更していない。

## テスト結果

- 既存公開CLI結合に検証根拠の必須検査を追加し、欠落のRedを確認。保存/投影の接続後にGreen。
- 完了後に成果物を変更してnextを実行しても、記録済みの検証根拠が変化しない公開結合1件が成功。
- 固定本家の比較は配列整列差でRed。artifactとproducerの結合キーに対するNULを無視する照合へ修正し、22観測が一致した。
- intent_lifecycle全122件が成功。この後の共有コード変更は再確認が必要。
- 静的検査でjson!経由の禁止変換を検出し、Valueの明示組立とcanon_jsonによる正準化へ修正した。変更後の最終Clippyは実施中。

ログはtest-evidence/validation-*.log。並行実装の未完了enum/exportによるコンパイル失敗は、機能欠落を再現したRedに数えない。

## 未完了と確認範囲

この記録はValidation Basisの採取・保存・描画の限定結果。次工程のSource Baseline、jumpの無効化情報、対象外センサー監査の裁定は別に残る。旧GateApprovedを直接組み立てる投影ゴールデンの入力移行は未了で、状態/監査18件全成功とはしない。単位付き実行の全DAG解決や任意スコープの完全互換は今回の22観測だけで保証しない。レビュー受領の内容結合や全workspace/カバレッジ/CI/実地スモークの完了を代替しない。

## 監査欄順と既存受入の追加確認

採取済みのValidation Basisを投影入力にした2ケースで、STAGE_COMPLETEDの欄順が本家と違うRedを検出した。Stage → Validation Basis（またはWarning）→ Detailsの順へ是正した。根拠の採取そのものは本家22観測との比較で検査し、投影テストでは保存事実を描く責務を検査する。

jump担当による保存観測の追加も合わせ、projection_golden_testは18件中16成功・2失敗となった。2失敗は未回答のセンサー監査3種だけであり、除外して成功扱いにはしていない。一般の直接jumpの受理仕様は別の人間裁定が必要で、投影の成功とは分ける。

Queryの既存テストに、明示--resumeが再開メニューを返す旧期待が残っていた。本家固定ソースのpark解除分岐と通常継続、および更新済みのドメイン契約を照合し、現在工程・gate・RunStage参照も含めて正しい直接再開の期待へ移行した。DAO全34件が成功。製品の動作をこの検査修正で変更していない。

JSON組立の静的検査違反は、規則を緩めず既存canon_jsonへ統一して是正した。共有編集終了後の最終Clippyは引き続き必要。
