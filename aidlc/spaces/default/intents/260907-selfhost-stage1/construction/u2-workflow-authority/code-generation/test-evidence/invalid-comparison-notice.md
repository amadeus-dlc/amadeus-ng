# 無効な比較証拠

`invalid-raw-number-human-parity.json` は、修正前の採取物を時系列保存したものであり、互換性の根拠として使用しない。

本家実行前にnativeのresponseファイルを残していたため、raw文字列番号1を本家フックが捨てて何も作らなかった場合も既存バイトで一致したように見えた。本家実行前の不存在と実行後の新規生成を確認する比較へ是正する。文字列番号の保持は承認済み修正であり、未修正本家と同じ挙動だという主張は撤回する。JSON数値1の受理へ対象を広げない。
