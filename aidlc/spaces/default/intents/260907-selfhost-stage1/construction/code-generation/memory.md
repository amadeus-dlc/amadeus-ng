<!-- INVARIANT: examples are single-line HTML comments so a fresh template parses to total=0 (MEMORY_EMPTY). Do NOT un-comment or split across lines. t100 guards this. -->
> This file is kept up to date automatically while the stage runs. Add observations at the review step, not by editing here directly.

## Interpretations
<!-- example: 2026-05-29T10:14:32Z — chose REST over GraphQL; the consuming team only needs CRUD, revisit if subscriptions land -->
- 2026-09-14T00:02:04Z — Repository ポートを全数点検した（11 トレイト）。`store` 以外の書き換え動詞を持つのは `CodekbRepository::recover_interrupted_publications` の 1 件だけで、これは今回私が足したものである。追加動詞を持つ他の 3 本（`IntentRepository::find_for_execution`、`IntentExecutionRepository::find_for_approval_origin`、`WorkflowDefinitionRepository::find_for_intent`）はいずれも読取で、`gateway-taxonomy.md` §2b が参照関係による named retrieval として明示的に許可している。`intent_execution_repository.rs` の残りの関数は `#[cfg(test)]` の契約テストでトレイト面ではない。
- 2026-09-13T23:27:15Z — 群 A の来歴欠陥の是正を実測で確認した。採取スクリプトはピンを `git archive` で一時ディレクトリへ実体化して実行する方式になり、実体化できなければ失敗する（作業ツリーへ暗黙に後退しない）。ピン vs 既存ゴールデン、ピン vs 作業ツリーはいずれも 0/16 差分で、方式変更の前後でゴールデンのバイトは 1 件も変わらなかった。`801c5700` がピンへ加えた差分は `.kimi-code` ハーネスの追加のみで、群 A の 6 動詞の経路に触れていない。
- 2026-09-13T10:52:09Z — 上流との失敗経路の差を、利用者の裁定により既知の限界として別 Bolt へ繰延した。本家は未知 slug 等の失敗時に stderr へ `{"error":..}` エンベロープを出し、状態ファイルが存在するときは `ERROR_LOGGED` を監査へ書く。native は plain 文言で監査副作用を配線していない。標準出力空・終了コード 1 は一致しており、bugfix スモークは有効 slug で呼ぶため踏まない。切替条件 2（状態・監査の upstream 互換）の判定前に要裁定として残る。
- 2026-09-13T10:52:09Z — 群 A/B/C の CLI ゴールデンは合成フィクスチャに対する採取であり、実グラフ 33 ノード・実 scope でのバイト一致は証明していない。実データの版差（現行 `.claude` に `selfhost-stage1` scope があり `a277af21` に無い）を避け、描画フォーマットのバイト一致を検証する範囲と読んだ。証明範囲の限界として記録し、完全互換の根拠へ拡大解釈しない。
- 2026-09-13T00:40:00Z — U4 の実測で「本リポジトリの doctor を green にするには記録を native エンジンが駆動している必要があり、そのためには未配線 8 件が要る」という循環が見えた。残る失敗 2 件（D4.c / D5.b）は接続の問題ではなく、配布 TypeScript のエンジンが native の実行カーソルを作らないことが原因である。切替条件 1 と 4 が同じ根に掛かっていると読み、U4 の完了条件（準備が揃うこと）とは切り離した。unit-of-work U4 が「準備完了だけで FR7・FR8 を達成扱いにしない」と明示しているため、この未達は U4 の失敗ではない。
- 2026-09-12T05:20:00Z — 裁定 Q3 = A の括弧書き後半「記録があるときは既存ストアへ追記」を、承認済み契約より弱い根拠として扱った。DC10 の契約テストが空間ストアの `journal` へのあらゆる INSERT を ABORT させたうえで報告全文と行数不変を要求し、C7 の `effects` が `initialized_record: HEALTH_CHECKED_via_U2_command_and_RMU` と定めているので、両立しない。選択肢を書いた私の側の誤りであって上流と実装の不一致ではないと読み、Q4 として人間へ再提示した（回答 A、現状追認）。担当の申告をそのまま採らず、DC10 の実ソースと C7 の `effects` を自分で読んでから提示した。
- 2026-09-12T00:10:00Z — U3 の裁定 Q2（「コマンド側集約が処理してイベントを吐き出し、RMU がイベントからリードモデルを作り、クエリ側がその結果を表示」）を、観測そのものの移設までは求めていないと読んだ。観測（ファイル・環境・ストアの読取）は「読むだけ」であり、`cqrs-boundaries.md` 規則 5 がコマンド側への配置を禁じる。よって観測はクエリ側 DAO のまま残し、クエリ View → ドメイン観測の写像を合成ルート（`modules/app/aidlc/src/runtime/doctor.rs`）へ置く。両側を知ってよいのは RMU と合成ルートだけ、という同規則の帰結である。移すのは判定・集計・文言だけとした。
- 2026-09-12T00:10:00Z — 集計（`passed` / `failed` / 終了コード）も RMU が焼き込む対象と読んだ。規則 6 の 2026-09-02 追記は「クエリ側ユースケースは DAO で View を読んで返すだけ、行に無い事実を作った時点で CQRS 違反」と定める。行を数える処理はクエリ側に残せば「行に無い事実を作る」に当たるため、非正規化リードモデルへ含める。
- 2026-09-10T18:35:00Z — 裁定 jump-redo-guard Q1 = A（モデルを実装へ揃える）の射程を、集約 `jump_execute_guard` が別 scope 無しの跳躍へ課す `jump_plan(None)` = 実効 EXECUTE と読んだ。よって Quint 側のガードは `inScope(cursor)` 一本とし、`synced` と `foreign` を個別に除外する形にはしていない。両方とも「実効計画の外にカーソルがある」という同じ帰結を持つためである。
- 2026-09-10T11:05:00Z — `.claude/settings.json` の `env` / `model` / `effortLevel` 22 行の削除は、利用者の回答「BEDROCKは使いません」と、ファイルの更新時刻（2026-09-10 05:23Z、短いセッションが連続した直後で、どのセッションの監査にも書込みが無い）から、利用者自身の意図した変更と読んだ。同ファイルは同期ツールの保持設定（`PRESERVE`）で、ツールが書き替えたものではない。作業ツリーの内容をそのまま B1 に含め、U2 の実装変更ではなく利用者の設定変更として code-summary に明記する。`CLAUDE_CODE_USE_BEDROCK` は未設定でも既定で無効であり、Bedrock を使わない方針と矛盾しない。
- 2026-09-10T05:55:00Z — 委譲 brief への規則束の逐語貼付けを、`.claude/settings.json` に登録済みの `aidlc-deliver-stage-rules.ts`（Task の PreToolUse で同じ束を逐語で付ける）に委ねた。同じ束が 2 度届くのを避けるためであり、brief には規則束の所在（`implementation-brief.md`）と、承認済み計画・テスト手順の全文、`AIDLC-UNIT` / `AIDLC-TESTING-CONTRACT` の先頭 2 行を載せた。

## Deviations
<!-- example: 2026-05-29T10:14:32Z — skipped the optional caching layer the stage prose suggested; the dataset is small enough that it adds risk -->
- 2026-09-14T01:54:42Z — オーナー裁定: `use-case-domain-getter` を lint 射程外にする案は**却下**。規則を緩めて通す提案は不可。`find_for_*` 3 本の是正は本 intent では扱わず、別セッションへ切り出す（プロンプトを提供済み）。U2 は `find_for_*` に触れずに残りを進める。
- 2026-09-14T01:01:19Z — オーナー裁定: Repository の検索メソッドは検索鍵を型で明示する。`find_for_intent(&self, intent: &Intent)` は Intent のどの属性で引いているかが自明でないため、`find_by_intent(&self, intent_id: &IntentId)` の形にする。対象は既存 3 件（`WorkflowDefinitionRepository::find_for_intent` / `IntentRepository::find_for_execution` / `IntentExecutionRepository::find_for_approval_origin`）。これは `gateway-taxonomy.md` §2b 88 行目が 2026-09-05 裁定として集約を渡す形を明示的に許可している記述と矛盾する。オーナーに確認し「今のコメントが正しい」と再確認を得たので、**規則本文のほうを改訂する**。規則の書込は §13 学習儀式が正規経路なので、工程ゲートで載せる。放置すると正典とコードが食い違い、次の担当が規則どおり書いて元へ戻る。あわせて識別子型の命名も是正中（`CodekbRepoName` → `CodekbRepoId`。このコードベースの識別子型は例外なく `<なにか>Id` で、`*Name` は 1 つも無い）。
- 2026-09-14T00:04:56Z — オーナー裁定（続き）: 「書き込みなら store メソッドにして、引数は集約を渡せ」。復旧をアダプタの名前付きコンストラクタへ隠す私の案も棄却された（構築に書込を隠す形で、結局隠蔽になる）。正しい形は ES リポジトリの通常の流れである — `find_by_id` で畳む前の姿を観測し、中断した公開が在ることを集約の状態として持ち、畳むのは集約の振る舞いでイベントを生み、`store(event, aggregate)` で永続化する。rename は `store` 実装の内部詳細。ポートは `find_by_id` と `store` の 2 語彙のみ。私は同じ論点で 3 度設計を外しており、置き場を独断で決めずに裁定を仰ぐべきだった。
- 2026-09-14T00:02:04Z — 停止させられたレビュアーが計画へ判定行だけの部分的な `## Review` を書き残していた（findings も検証結果も無し）。規約上これは未完の試みで判定は存在しないため、削除して計画を承認時の 30984 バイトへ戻し、`REVIEW_COMPLETED` は記録しなかった。findings も検証も無く人手で停止させられたものを受領すればレビュー受領の捏造になる。なおこの部分追記は承認指紋を壊し、記録ディレクトリ外への変更操作をガードが一律に止める状態を生んでいた。復元で解消した。
- 2026-09-13T23:27:15Z — 群 D の初版は中断トランザクションの復旧（ディレクトリ rename）を `CodekbRepository::find_by_id` の内側に隠しており、読取メソッドが副作用を持つ形だった。利用者の裁定「find_by_idに書き込みが隠れている？CQS違反だろそれ。」により是正した。ポートへ `recover_interrupted_publications`（`&mut self` + `Result<(), E>`）を独立コマンドとして新設し、`find_by_id` を `observe_generation` だけの純粋な読取へ戻し、両ユースケースがロック区間の先頭で明示的に復旧を呼ぶ上流と同じ順序にした。採取 24 ケースのバイトと `tree_hash` の上流実測 3 ベクタは不変。現状維持案を選択肢に並べた私の提示が甘く、規則違反を許容する形になっていた。
- 2026-09-13T10:52:09Z — `codekb-scope-diff` を承認済み計画は「どのモード・どの verdict でも exit 0」としたが、ピン `a277af21` の実測では verdict 経路は exit 0、usage 経路（`--mint` の `--paths` 欠落、`--compare` の対象ファイル欠落）は `die()` で exit 1 だった。計画文言より上流実測を優先し upstream-exact で実装した。計画は凍結成果物なので本文を書き換えず、逸脱として記録する。担当が独自に読み替えず先に提示した点は規則どおり。
- 2026-09-13T10:52:09Z — 群 A の採取スクリプト `scripts/goldens/capture-workflow-authority.ts` は provenance に `upstream_commit: a277af21` を記すが、実際には submodule worktree（fork `801c5700`）配下の配布ツールを実行しており、ピンとの一致検証を持たない（定数を来歴へ書くだけ）。該当ハンドラと lib ヘルパのバイト一致は群 B/C 担当が独自に確認済みだが、来歴の主張が未検証のため欠陥として扱い、ピンを `git archive` で実体化して再採取する修正を行う（U1 の R-02 と同じ考え方）。
- 2026-09-13T00:40:00Z — U4 の承認済み計画に実測の誤りが 3 件あり、担当が是正した（`aidlc-testing-posture` の 2 動詞は native 配線済みだった／`aidlc-audit append` の 4 箇所は禁止の記述で呼出しではない／`claude-without-bedrock.patch` の不一致という疑いは配布側を見ていた私の誤読）。計画は凍結成果物なので本文を書き換えず、code-summary の「承認済み計画からの逸脱」に記録した。計画段階の実測を grep の見た目だけで確定させたのが原因で、呼出しか記述かの区別と、パッチの適用先の確認を省いていた。
- 2026-09-13T00:40:00Z — 計画の 3 前提が同時に成立しないことを、担当が live を書き換える前に実測で示して裁定へ回した。承認済み計画に従って live を書き換えていれば、この build の自己診断が落ちる状態を作っていた。計画の前提を実装前に接続の形ごとに実測する（`harness_binding_contract` の表）という進め方が効いた。
- 2026-09-12T00:05:00Z — セッション再開でディレクティブが再発行され、U3 の計画承認（指紋 `sha256:5a235659…`、Runtime Session `9d1d85aa`）が失効した。計画本文・Testing Contract（`contract_sha256` は `sha256:d904f82d…` のまま render と一致）・テスト手順を無変更のまま `[Answer]:` を空へ戻し、指紋 `sha256:732060c8…` で承認を取り直した（Runtime Session `45553ef9-4273-4beb-99de-ddf1307c390e`、`PLAN_APPROVAL_RECORDED` まで通過）。U2 でも再開のたびに同じ取り直しが起きており、これで 4 回目である。
- 2026-09-12T00:10:00Z — 承認済み `code-generation-plan.md` §「責任と配置」2 は D1–D5 の判定を Query use case に置くが、裁定 Q2 によりこれは計画側の誤りと確定した。計画は凍結成果物なので本文を書き換えず、逸脱と目標の配線を `relocation-brief.md` に記録して担当へ渡した。計画の 1・3・4 は有効のまま。
- 2026-09-10T13:40:00Z — 前セッション（Runtime Session `9d1d85aa` の前、`27e03be0-…`）が 2 担当（`u2_classic_parity` / `u2_hook_parity`）の作業途中（21:02 頃）で止まり、報告書は未作成のまま作業ツリーに両担当の未完変更が残った。再開でディレクティブが再発行され承認（指紋 `f1d7f6e0…`）が失効したため、計画本文・Testing Contract・テスト手順を無変更のまま指紋 `sha256:b83cf443…` で承認を取り直した。1 回目の `decision` に SessionStart の Runtime Session ではなく別の識別子を渡したため受領が拒否され、同じ質問を 2 回提示した（利用者はいずれも Approve Plan）。再開直後の実測: `cargo check --workspace --all-targets` 成功、`cargo fmt --all` で 5 ファイルを整形、`cargo test --workspace --no-fail-fast` は 3,098 件成功・25 件失敗（全文ログは scratchpad `ws-test-1.log`、後で step9-logs へ写す）。
- 2026-09-10T13:40:00Z — 25 件の失敗が両担当の所有ファイルにまたがり（`directive_drawing` / `turn` / `classic_corpus_contract` と `claude_hook_contract` / `upstream_271_contract` の stop 系 / `task_update_contract` / `next_branches` / `learnings_contract` / `runtime_graph_contract` / `intent_lifecycle`）、並行させると再び同じテストファイルで衝突するため、残作業を 1 担当（`u2_parity_closeout`）へ直列でまとめ直した。
- 2026-09-10T10:40:00Z — 利用量上限で前セッションが止まり、再開でディレクティブが再発行されて計画承認（指紋 `a22159d4…`）が失効した。計画本文・Testing Contract・テスト手順を無変更のまま `[Answer]:` を空へ戻し、指紋 `sha256:f1d7f6e0…` で承認を取り直した（Runtime Session `27e03be0-…`、AskUserQuestion の選択が `HUMAN_TURN` として記録され、`PLAN_APPROVAL_RECORDED` まで通った）。前セッション終盤の 3 担当（classic 是正・フック差是正・差し向け記録）は資料読込の段階で止まっており、作業ツリーへの変更は 1 件も無かった（各 subagent transcript に Edit/Write が無いことを確認）。再開直後の fmt / clippy / `cargo lint` / verify-corpus / aidlc-sync --check はすべて成功。
- 2026-09-10T18:35:00Z — ベースラインの `cargo test --workspace --no-fail-fast` を `tail -120` で捕捉したため、全スイートの件数を採取できていない。exit 0 は確認済みだが、Step 9 の最終記録には全文を残す実行をやり直す必要がある。
- 2026-09-10T05:55:00Z — セッション再開でディレクティブが再発行され計画承認が失効したため、計画本文・Testing Contract・テスト手順を無変更のまま `[Answer]:` を空へ戻し、指紋 `sha256:a22159d4…` で承認を取り直した（Runtime Session `8f466f39-…`）。今回は構造化回答（AskUserQuestion）の選択が `HUMAN_TURN` として記録され、チャットへの直接入力なしで `PLAN_APPROVAL_RECORDED` まで通った。前セッションの「構造化回答では受領へ結ばれない」という記録は現在のフックでは再現しない。
- 2026-09-10T05:55:00Z — Step 7 の未完了 3 スライス（fold-usage 実装、deliver-stage-rules と reviewer-scope の着地記録）と Step 8 の棚卸しを 4 名へ並行委譲した。前セッションの記録（共有署名の変更でコンパイル停止）を踏まえ、`runtime.rs` と `intent_execution_event.rs` の所有を固定し、他担当のファイルへ触れないよう brief に明記した。

## Tradeoffs
<!-- example: 2026-05-29T10:14:32Z — picked TDD over BDD this run; the team is unit-first and the domain is well-understood -->
- 2026-09-12T00:30:00Z — U2 計画の `- [ ] Step 9` を未チェックのまま残した（利用者の裁定）。R-01 は「Unit 完了・B1 統合の時点で親が更新する」と提案したが、同ファイルはレビュー対象成果物で終端 `REVIEW_COMPLETED` の `Artifact Fingerprint: sha256:1cf77486…` に束縛されており、U2 の回復レビュー 1 回は iteration 2 で使い切っている。チェック 1 文字のために工程ゲートの停止を招くより、完了の根拠を `progress-status.md` に明記して閉じる方を採った。R-01 は iteration 1 の所見で、終端受領の成立前に書かれている。
- 2026-09-10T10:40:00Z — 前セッションの 3 担当分を 2 担当へまとめ直した。フック差の是正（監査値の `<project-dir>` 置換）と差し向け記録の逐語強制は、どちらも `ReviewerScopeBlock` の `Target` / `Stage` / `Unit` と `review_guards_contract.rs` の期待値を書き替えるため、並行させると同じテストファイルで衝突する。Step 8 の classic 是正は別担当のまま、`steering_source.rs`（`bundle` の接頭辞と規則 BOM の両方が触る）も classic 側の所有に寄せた。R7（旧ピン・逸脱台帳を根拠にする注記）は是正、R9（intent 記録内 fixture の `include_str!`）は `tests/golden/selfhost-stage1/` へ移す、と親が判断した。
- 2026-09-10T18:35:00Z — 残る Step 7 のフック接続を 2 担当の並行委譲にした。前セッションで共有署名の変更がコンパイル停止を招いた記録があるため、3 担当以上へは広げず、`run_hook` のフック名一覧を同時に触ることを両者へ明示した。競合の解消は親が行う。
- 2026-09-10T07:20:00Z — 本家との差を「契約 C1 / C2 / C3 で是正が決まるもの」と「契約だけでは向きが決まらないもの」に分け、前者は裁定なしで担当へ是正を出し、後者だけを構造化質問で人間へ回した。全件を裁定に回すと U2 の着地が遠のき、全件を独断で直すと Mandated「上流との不一致を人間へ提示」に反するためである。
- 2026-09-10T07:20:00Z — 2.7.1 コーパスの未駆動ケースは、先頭 4 ケース（前提を probe で再現できたもの）だけを U2 でテストへ固定し、承認・jump・skip を挟む連鎖が要る残りは切替条件 2 の判定材料として一覧で記録する案を推奨し、利用者が選んだ。U2 の着地を優先した判断であり、Step 8「駆動できないままの必要ケースを見直す」を「見直して分類し、判定前に駆動するかを決める」と読んだ。

## Open questions
<!-- example: 2026-05-29T10:14:32Z — confirm the retention window with compliance before the next stage hardens the schema -->
- 2026-09-14T01:54:42Z — オーナー指摘: インメモリ Repository が複雑な内部構造を持っており、DB 化の障壁になる恐れがある。`find_for_*` の是非よりこちらが本質的な問題との見立て。私は未検証で、別セッションの調査対象。
- 2026-09-14T00:02:04Z — オーナー裁定（Forbidden 級）: Repository の語彙は集約を I/O する `find_by_id` / `store` / `delete_by_id` のみ。`recover_interrupted_publications` は禁止。§13 の学習儀式で `memory/` へ載せる候補として記録する。中断公開の復旧は Repository の責務ではなく、ロックを取っている合成ルート側へ移す（上流も `withCodekbLock` の直下で `recoverCodekbTransactions` を走らせる）。`infrastructure-layer.md` は「汎用だから」と Gateway をその層へ置くことを禁じているので `core-infrastructure` は置き場にしない。
- 2026-09-13T23:27:15Z — `find_by_id` に副作用が隠れている面が他の Repository にも無いか、横断点検が要る。今回の射程外であり、`project.md` の規則により AI の判断だけで GitHub Issue を起票しないため、人間の裁定を待つ事項として記録する。
- 2026-09-10T10:50:00Z — 作業ツリーの `.claude/settings.json` は `main` に対して `env`（`CLAUDE_CODE_USE_BEDROCK` 等 17 変数）・`model`・`effortLevel` の 22 行が削除されている。誰がいつ消したかは記録に無く、U2 の実装対象でもない。B1 の PR に含める前に利用者へ提示して裁定を求める（`settings.local.json` へ移した意図的な変更か、担当の誤編集か）。
- 2026-09-10T10:50:00Z — U2 は U1 が申告した経路（`scripts/goldens/capture-cli.ts`、`.github/workflows/ci.yml`、`tests/golden/upstream-a277af21/` の再封印と `reviewer-scope/` 追加、記録用フック 3 本）を U1 完了後に変更している。U1 の終端レビュー受領がこの書込みで失効しているかは、Unit 完了・工程ゲートの時点でエンジンの判定に従い、失効していれば U1 の回復レビューを 1 回起票する。
- 2026-09-10T18:35:00Z — 計画外カーソルへの redo について、本家 2.7.1 の該当観測は未確認のままである。裁定 A は「実装を正とする」判断であって上流観測との突き合わせではない。切替条件 2（状態・監査の upstream 互換）を判定する前に、この 1 点を実観測で確かめるかどうかは未決である。
- 2026-09-10T07:20:00Z — 委任エージェントは `aidlc-state-transition-guard` により本家の `aidlc-orchestrate.ts` も本 build の `aidlc next` も走らせられない（一時ワークスペース対象でも一律拒否）。本家との A/B 実走行は親が主セッションで行うしかなく、今回は probe スクリプトで代行した。ガードの判定に `--project-dir` の宛先を見せるかは配布元の変更であり、本 intent では扱わない。
