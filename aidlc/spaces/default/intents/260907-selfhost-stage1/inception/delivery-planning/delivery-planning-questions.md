# 実行計画の確認

## 確定済みの前提

- [要求書](../requirements-analysis/requirements.md)、[4単位の定義](../units-generation/unit-of-work.md)、[依存関係](../units-generation/unit-of-work-dependency.md)、[要求対応表](../units-generation/unit-of-work-story-map.md)、[契約書](../contract-design/contract-summary.md)、[開発規則](../practices-discovery/team-practices.md)を引き継ぐ。
- 2.7.1のゴールデン・検証内容・実装を揃え、CQSの例外を設けない。更新はイベント追記、結果はRMUが投影しクエリAPIで取得する。
- 作業は直列、通常の作業単位で進める。独立した先行実装や自律実行、複数チームによる並行作業は追加しない。
- 各Bolt（実装・検証・統合までのまとまり）を [Pull Request](https://github.com/amadeus-dlc/amadeus-ng/pulls) 1本に対応させ、CI全ジョブ成功とレビュー収束後にsquash-mergeする。
- 最終的な実地スモークと切替は、作成した準備資産だけで達成扱いしない。

## Q1: 最初の統合に採取基盤と対応実装をまとめるか

4単位を次の3つのBoltにまとめる案を提示する。最初に本家2.7.1との観測差と、状態・監査・承認の成立を確認する。依存先を先に実装しつつ、採取だけでなく対応実装まで揃えて統合する方針である。

| 順 | Bolt | 含む単位 | 統合時の完了条件と確認できること |
| --- | --- | --- | --- |
| B1 | 2.7.1の受入基盤と作業進行 | U1・U2 | 本家から採取した基準、対象実装、テストの参照先・検証内容を2.7.1へ揃える。必要なCLI・フック・承認・監査と、CQSを守る報告結果取得を確認する。旧期待値で移行済みとしない |
| B2 | セルフホスト用の自己診断 | U3 | 契約C7の採用項目、初回/既存記録、正常/異常、助言、終了コード、診断監査の契約を検証する。実配線での最終確認はB3後の実地検証へ残す |
| B3 | 配布接続・切替準備 | U4 | Rust更新処理と再利用補助処理の接続、副作用、配布資産、スモーク実行準備、ホスト識別と復帰準備を検証する。実地完了とタグへの切替は後続工程で確認する |

B1内でU1の採取基盤が揃っても、U2の対応実装と検証が通るまでは統合しない。U1の確定したコーパスをU2が消費し、変更が必要なら採取元と差分を確認して該当する検証をやり直す。正規化の拡大、検査の省略、古い期待値の維持でCIを成功にしない。

この案は最初の変更量が大きくなる。一方、採取を独立したBoltにする4回案は早く採取基盤を確認できるが、対応実装の統合はその次になる。どちらでも4単位と要求範囲は変えない。

- A. U1とU2をまとめ、3回で統合する（推奨） — 最初の統合で採取基準と対応実装・検証を揃える。
- B. 1単位ずつ、4回で統合する — 採取基盤を先に独立して確認し、続く統合で対応実装・検証を移行する。最初の統合を2.7.1適合の達成とは呼ばない。
- X. Other (please specify)

[Answer]: A. U1とU2をまとめ、3回で統合する

**User Input**: 1
**Mode**: guided

## 実行時の検証と外部依存

- 実装前に現行テストの基準値を取り、各変更をTDDで進める。各統合でfmt・clippy・独自lint・単体/統合テスト・Quint/ITF・カバレッジ・配布同期・依存監査・レビュー収束を確認する。
- CIの集約成功だけに頼らず、`audit`を含め各ジョブを確認する。現行イベント条件で非適用のジョブは成功に書き換えない。
- `main`とHEADはともに`1dc727e00a26c27258d916cb3a5e0592c6c48c1c`。現在の`stage1`ブランチに、この作業記録と、利用者が是正を指示した自動検査の同期パッチ・回帰テストがある。実装用ブランチへ引き継ぎ、無関係な変更として捨てない。
- ローカルとリモートのタグ一覧は空だった。開発中は現在の配布済みホストを使用し、最初のネイティブ安定タグは実地スモーク・自己診断・CI成功の対象コミットを確認してから決める。未検証の版を安定タグと呼ばない。
- Bun、Cargo、Claudeのコマンド入口はローカルに存在する。Claudeの対話実行・人間の回答と承認、GitHubのCI・レビューの応答は外部依存として記録し、未確認の応答時間や完了日を約束しない。
- 実地スモークで使う小さな修正と作業記録、安定ホストの具体的な参照先は、実行前に現物に基づいて特定する。現在の作業記録を壊す切替はしない。

## Sources

- [scope] Workflow-selected scope: selfhost-stage1、Minimal。
- 本文冒頭の承認済み成果物6件。
- `.github/workflows/ci.yml`の7ジョブと集約条件。
- `git status --short --branch`、`git rev-parse HEAD main`、`git tag --list`、`git ls-remote --tags origin`の2026-09-07実測。

## Consolidated Summary Confirmation

- 4単位を3つのBoltにまとめ、B1（U1・U2）→B2（U3）→B3（U4）の順で直列に進める。1回の統合を1本の変更レビューに対応させ、mainへsquash-mergeする。
- B1は本家2.7.1の採取基準と、対応する実装・テストの移行を一緒に統合する。U1の採取が完了しても、U2の対応と検証が通るまでは統合しない。reportの結果取得はイベント追記・RMU・クエリに揃え、CQSの例外や旧い戻り値経路を残さない。
- B2はC7の採用診断項目とDC1–DC10を実装・検証する。診断のQueryと、診断実施の監査を記録する更新コマンドを分ける。
- B3はRust更新処理と再利用補助処理の接続、副作用の確認、配布資産、スモーク実行とホスト識別・復帰の準備を整える。
- 各単位をTDDで実装・検証してから次の単位へ進む。担当はこの作業を進める単一の開発担当を基本とし、別チーム・並列実行・自律実行・独立した先行実装は追加しない。
- 各統合でCI全ジョブ成功とレビュー収束を確認する。既存のカバレッジ床90%、相対ゲート、Quint/ITF、fmt・clippy・独自lint、配布同期を維持する。既存の自動検査是正と作業記録も引き継ぐ。
- 本家2.7.1の比較基準と更新経路を早く検証する方針を採る。形式的な点数付けや、依存関係に反する先行実装はしない。B1の差分量が大きくなるリスクと、採取/実装を同時に整合させる利点を記録する。
- 4単位の準備が終わった後、全体検証と本リポジトリでの実地スモークを行う。人間承認・自己診断・CI成功を確認して、初回のネイティブ安定タグとホスト参照先を確定する。準備完了だけで切替完了とはしない。
- 現在の配布済みホストを開発中の進行に使い、切替後は安定タグのホストと開発版を使い分ける。外部の対話・CI・レビューに必要な待ちを記録し、未確認の期日や待ち時間を約束しない。
- 実行計画、担当割当、順序とリスクの根拠、外部依存の4文書を作成する。実装に入る前に要求対応の検証をまとめ、未解決の対応漏れがないことを確認する。

この内容で実行計画を作成する前に、認識に相違がないか確認してください。

- Looks correct
- Request changes

[Answer]: Looks correct
